iterations: 3
episodes_per_iter: 2000
max_steps_per_episode: 1000
width: 10
height: 20
seed: 0
device: cuda
critic_ckpt: dqn_updated.pt
init_ckpt: checkpoints/plan_denoiser.pt
from_scratch: false

horizon: 8
stride: 1

top_episode_pct: 0.10
advantage_quantile: 0.80
board_health: true
min_steps_per_episode: 1

rerank_mode: dqn
sampling_constraints: mask_logits
invalid_handling: penalize
invalid_penalty: 1000000.0
num_candidates: 32
sample_steps: 8
temperature: 1.0

train_epochs: 3
train_batch_size: 256
train_lr: 0.0003
train_mask_prob: 0.5
train_d_model: 128
train_layers: 4
train_heads: 4

eval_episodes: 50
eval_max_steps: 2000

beam_eval_episodes: 50
beam_eval_max_steps: 2000
beam_width: 16
beam_horizon: 3

diversity_enable: false
diversity_buckets: 4
diversity_per_bucket_cap: 10

output_dir: runs/selftrain_default
